{
  "name": "expressapp",
  "version": "1.0.0",
  "dependencies": {
    "config": "^3.3.7",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
